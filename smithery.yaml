# Smithery configuration for nishati-mcp
# East Africa AI Coordination Infrastructure — AI-KungFU
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |-
    (config) => ({
      command: "nishati-mcp",
      args: [],
      env: {}
    })
